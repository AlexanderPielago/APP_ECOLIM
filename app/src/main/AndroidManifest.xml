<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ecolim_app">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ECOLIM_APP">

        <activity
            android:name="Vista.LoginView"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="Vista.MenuView"
            android:exported="false" />
        <activity
            android:name="Vista.RegisterView"
            android:exported="false" />
        <activity
            android:name="Vista.TypeWasteView"
            android:exported="false" />
        <activity
            android:name="Vista.ReportView"
            android:exported="false" />
        <activity
            android:name="Vista.RecordView"
            android:exported="false" />
    </application>

</manifest>
